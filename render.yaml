services:
  - type: web
    name: eda-music-frontend-static
    env: static
    plan: free
    autoDeploy: true
    staticPublishPath: static
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
